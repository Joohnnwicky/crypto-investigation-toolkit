# Crypto Investigation Toolkit Dependencies
# Install: pip install -r requirements.txt

Flask==3.1.3

# requests is already installed (2.32.5) but listed for completeness
requests>=2.32.0

# web3 for Ethereum RPC queries (Uniswap, Tornado Cash tracing)
web3==7.15.0

# pandas is already installed (3.0.1) but listed for future tools
pandas>=3.0.0