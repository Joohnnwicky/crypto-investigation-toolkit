---
phase: 05-case-handling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/case/routes.py
  - modules/case/asset_freeze_generator.py
  - templates/case/asset_freeze.html
autonomous: true
requirements: [CASE-03]
user_setup: []

must_haves:
  truths:
    - "User can fill 3-step form: case info, address selection, freeze details"
    - "Template displays with all 4 field categories (case info, target info, reason, terms)"
    - "User can copy template text to clipboard"
    - "User can import addresses from monitor or cluster results"
    - "Template generation validates required fields"
  artifacts:
    - path: "modules/case/asset_freeze_generator.py"
      provides: "Asset freeze template generation logic"
      exports: ["generate_freeze_template_web"]
    - path: "modules/case/routes.py"
      provides: "Flask Blueprint routes for asset freeze"
      contains: "@case_bp.route('/api/asset-freeze/generate')"
    - path: "templates/case/asset_freeze.html"
      provides: "Step-by-step form UI with 3 steps"
  key_links:
    - from: "templates/case/asset_freeze.html"
      to: "/case/api/asset-freeze/generate"
      via: "fetch POST on generate click"
      pattern: "fetch.*api/asset-freeze/generate"
    - from: "templates/case/asset_freeze.html"
      to: "sessionStorage"
      via: "cit_monitor_export, cit_cluster_export, cit_obfuscation_export"
    - from: "modules/case/asset_freeze_generator.py"
      to: "modules/cross/cross_border_generator.py"
      via: "Same template generation pattern"
</context>

<objective>
Implement asset freeze template generator tool (CASE-03) with 3-step form, 4 field categories, and tool interconnection imports.

Purpose: Enable investigators to generate standardized asset freeze request templates with case information, target addresses, freeze reasons, and terms.
Output: Working template generator with backend validation, frontend step-by-step form, clipboard copy, and import from monitor/cluster/attack detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-case-handling/05-CONTEXT.md
@.planning/phases/05-case-handling/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From modules/cross/cross_border_generator.py (Template pattern):
```python
REQUIRED_FIELDS = [
    'case_number', 'agency', 'contact_person', 'contact_method',
    'suspicious_behavior', 'request_type', 'expected_response'
]

def validate_template_fields(fields: Dict[str, Any]) -> Dict[str, Any]:
    """Returns: {'valid': bool, 'missing': list}"""

def generate_plain_text(fields: Dict[str, Any]) -> str:
    """Returns: formatted plain text template"""

def generate_template_web(fields: Dict[str, Any]) -> Dict[str, Any]:
    """Returns: {'success': bool, 'template_data': {...}, 'plain_text': str}"""
```

From templates/cross/cross_border.html (Step-by-step form pattern):
```javascript
// Step navigation
function navigateToStep(step) {
    // Hide all sections, show current step
    // Update step indicators (bg-blue-600 for active, bg-gray-300 for inactive)
}

// Import from other tool
function importFromCluster() {
    const cached = sessionStorage.getItem('cit_cluster_export');
    // Extract addresses from clusters
}

// Copy to clipboard
async function copyTemplate() {
    await navigator.clipboard.writeText(templatePlainText);
}
```

From CONTEXT.md (Field categories D-34 to D-38):
```
D-34: 4 field categories: case info, freeze target info, freeze reason, freeze terms
D-35: Case info: case_number, agency, contact_person, contact_method
D-36: Freeze target: addresses, chain_types, freeze_amount, asset_type
D-37: Freeze reason: suspicious_behavior, fund_source, freeze_necessity, legal_basis
D-38: Freeze terms: freeze_duration, unlock_conditions, contact, follow_up
D-39: 3-step form: case info -> address selection -> template generation
D-40: Plain text copy export
D-47 to D-49: Import from monitor, cluster, or manual input
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create asset freeze backend module</name>
  <files>modules/case/asset_freeze_generator.py</files>
  <read_first>
    - J:\虚拟币犯罪调查工具集\docs\superpowers\modules\cross\cross_border_generator.py
    - J:\虚拟币犯罪调查工具集\docs\superpowers\.planning\phases\05-case-handling\05-CONTEXT.md
  </read_first>
  <action>
Create modules/case/asset_freeze_generator.py following cross_border_generator.py pattern:

1. Define REQUIRED_FREEZE_FIELDS per D-35, D-37:
   - 'case_number', 'agency', 'contact_person', 'contact_method' (D-35)
   - 'suspicious_behavior', 'freeze_necessity' (D-37)

2. Implement validate_freeze_fields(fields) returning {'valid': bool, 'missing': list}

3. Implement generate_freeze_plain_text(fields) per D-40:
   - Template title: "虚拟货币资产冻结申请"
   - Section 1: 案件编号, 调查机构, 联系人, 联系方式 (D-35)
   - Section 2: 冻结对象地址列表 (address + chain type), 冻结金额, 资产类型 (D-36)
   - Section 3: 可疑行为描述, 资金来源说明, 冻结必要性, 法律依据 (D-37)
   - Section 4: 冻结期限, 解除冻结条件, 后续处理 (D-38)
   - Footer: "本冻结申请由虚拟币犯罪调查工具集生成，仅供合规调查使用" + generated_time

4. Implement generate_freeze_template_web(fields) per D-34 to D-38:
   - Validate required fields, return error if missing
   - Build template_data dict with 4 sections:
     - 'case_info': {case_number, agency, contact_person, contact_method}
     - 'target_info': {addresses, chain_types, freeze_amount, asset_type}
     - 'reason_info': {suspicious_behavior, fund_source, freeze_necessity, legal_basis}
     - 'terms_info': {freeze_duration, unlock_conditions, follow_up}
   - Call generate_freeze_plain_text for clipboard copy
   - Return {'success': True, 'template_data': {...}, 'plain_text': str}

5. Implement detect_chain_types(addresses) helper:
   - Import detect_chain_type from modules/cross/chain_detector.py
   - Return list of chain types for each address

Use datetime.now().strftime('%Y-%m-%d %H:%M:%S') for generated_time.
  </action>
  <verify>
    <automated>grep -q "def generate_freeze_template_web" "J:/虚拟币犯罪调查工具集/docs/superpowers/modules/case/asset_freeze_generator.py"</automated>
  </verify>
  <done>
    - asset_freeze_generator.py contains all template functions
    - REQUIRED_FREEZE_FIELDS includes all mandatory fields from D-35, D-37
    - Plain text template follows D-34 to D-38 structure
  </done>
  <acceptance_criteria>
    - `grep "def generate_freeze_template_web" modules/case/asset_freeze_generator.py` returns match
    - `grep "REQUIRED_FREEZE_FIELDS" modules/case/asset_freeze_generator.py` returns match
    - `grep "虚拟货币资产冻结申请" modules/case/asset_freeze_generator.py` returns match
    - `grep "案件编号" modules/case/asset_freeze_generator.py` returns match
    - `grep "冻结对象地址" modules/case/asset_freeze_generator.py` returns match
    - `grep "冻结期限" modules/case/asset_freeze_generator.py` returns match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add routes and frontend UI</name>
  <files>modules/case/routes.py, templates/case/asset_freeze.html</files>
  <read_first>
    - J:\虚拟币犯罪调查工具集\docs\superpowers\modules\case\routes.py (from Plan 01)
    - J:\虚拟币犯罪调查工具集\docs\superpowers\templates\cross\cross_border.html
  </read_first>
  <action>
Add to modules/case/routes.py:

1. Add asset freeze routes to existing case_bp:
   - POST /api/asset-freeze/generate - accepts all fields, calls generate_freeze_template_web
   - GET /asset-freeze - renders templates/case/asset_freeze.html

Create templates/case/asset_freeze.html following cross_border.html pattern:

1. Extend base.html, title: "资产冻结申请生成 - CIT"
2. Step indicators (3 steps per D-39):
   - Step 1: 案件基本信息
   - Step 2: 冻结对象选择
   - Step 3: 冻结理由与条款
3. Step 1 section (D-35):
   - Fields: case_number, agency, contact_person, contact_method (all required *)
   - Grid layout 2x2
   - "下一步" button
4. Step 2 section (D-36, D-47 to D-49):
   - Import buttons: "从监控结果导入", "从聚类结果导入", "从攻击检测导入"
   - Manual address textarea
   - Address list display with chain types
   - Fields: freeze_amount, asset_type
   - "上一步" and "下一步" buttons
5. Step 3 section (D-37, D-38):
   - Fields: suspicious_behavior (required *), fund_source, freeze_necessity (required *), legal_basis
   - Fields: freeze_duration, unlock_conditions, follow_up
   - "上一步" and "生成模板" buttons
6. Template display section (D-40):
   - Pre-formatted plain text in font-mono style
   - "复制到剪贴板" button with success indicator
7. JavaScript:
   - navigateToStep(step) function (copy from cross_border.html)
   - importFromMonitor() reading cit_monitor_export
   - importFromCluster() reading cit_cluster_export
   - importFromObfuscation() reading cit_obfuscation_export
   - generateFreezeTemplate() posting all fields to /api/asset-freeze/generate
   - copyTemplate() using navigator.clipboard.writeText

Step indicator styling: active=bg-blue-600 text-white, inactive=bg-gray-300 text-gray-600.
  </action>
  <verify>
    <automated>grep -q "/api/asset-freeze/generate" "J:/虚拟币犯罪调查工具集/docs/superpowers/modules/case/routes.py"</automated>
  </verify>
  <done>
    - routes.py contains asset freeze routes under case_bp
    - templates/case/asset_freeze.html shows 3-step form
    - Import buttons for monitor, cluster, obfuscation results
    - Copy to clipboard button works
  </done>
  <acceptance_criteria>
    - `grep "/api/asset-freeze/generate" modules/case/routes.py` returns match
    - `grep "/asset-freeze" modules/case/routes.py` returns match (page route)
    - `grep "案件基本信息" templates/case/asset_freeze.html` returns match (step 1 title)
    - `grep "冻结对象选择" templates/case/asset_freeze.html` returns match (step 2 title)
    - `grep "冻结理由与条款" templates/case/asset_freeze.html` returns match (step 3 title)
    - `grep "从监控结果导入" templates/case/asset_freeze.html` returns match
    - `grep "从聚类结果导入" templates/case/asset_freeze.html` returns match
    - `grep "复制到剪贴板" templates/case/asset_freeze.html` returns match
    - `grep "cit_monitor_export" templates/case/asset_freeze.html` returns match
    - `grep "cit_cluster_export" templates/case/asset_freeze.html` returns match
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Backend | Template field values (text) cross this boundary |
| Frontend -> sessionStorage | Address lists from other tools imported via sessionStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Template fields | accept | Plain text output, no HTML injection possible; user copies to clipboard |
| T-05-10 | Information Disclosure | Generated template | accept | Template displayed on screen, user responsible for secure distribution |
| T-05-11 | Spoofing | Import data | accept | sessionStorage data comes from user's own previous tool usage within same session |
</threat_model>

<verification>
- Manual verification: Navigate to /case/asset-freeze, fill 3-step form, generate template
- Verify template appears with all 4 sections
- Verify clipboard copy works (shows "已复制!")
- Verify import from monitor/cluster/obfuscation works
- Verify error shown when required fields missing
</verification>

<success_criteria>
- User can fill 3-step form with all required fields
- Template displays with case info, target info, reason info, terms info
- Clipboard copy button works
- Import from monitor, cluster, obfuscation results works
- Template generation validates required fields
</success_criteria>

<output>
After completion, create `.planning/phases/05-case-handling/05-03-SUMMARY.md`
</output>