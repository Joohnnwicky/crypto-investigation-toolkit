---
phase: 01-core-framework
plan: 06
type: execute
wave: 3
depends_on: [01-03-PLAN.md, 01-04-PLAN.md]
files_modified:
  - run.bat
  - run.sh
  - requirements.txt
autonomous: true
requirements: [CORE-01]
user_setup: []

must_haves:
  truths:
    - "User can double-click run.bat to start Flask application"
    - "User sees Flask server running at http://127.0.0.1:5000"
    - "Browser opens automatically showing homepage"
  artifacts:
    - path: "run.bat"
      provides: "Windows startup script"
      contains: "python app.py"
    - path: "run.sh"
      provides: "Linux/Mac startup script"
      contains: "python3 app.py"
    - path: "requirements.txt"
      provides: "Python dependencies"
      contains: "Flask==3.1.3"
  key_links:
    - from: "run.bat"
      to: "app.py"
      via: "python command"
      pattern: "python app.py"
    - from: "run.bat"
      to: "browser"
      via: "start http://127.0.0.1:5000"
      pattern: "http://127.0.0.1:5000"
---

<objective>
Create startup scripts (run.bat for Windows, run.sh for Linux/Mac) and requirements.txt. These enable one-click local startup without manual configuration.

Purpose: Enable zero-config local startup per project goal
Output: run.bat, run.sh, requirements.txt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-framework/01-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md -->

Flask version: Flask==3.1.3
Python available: 3.14.3
pip available: 26.0.1
requests already installed: 2.32.5
pandas already installed: 3.0.1

App location: docs/superpowers/app.py
Working directory for scripts: docs/superpowers/

Server host: 127.0.0.1
Server port: 5000
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create requirements.txt</name>
  <files>J:\虚拟币犯罪调查工具集\docs\superpowers\requirements.txt</files>
  <read_first>
    - J:\虚拟币犯罪调查工具集\docs\superpowers\.planning\phases\01-core-framework\01-RESEARCH.md (for dependency versions)
  </read_first>
  <action>Create requirements.txt with Flask dependency:

```text
# Crypto Investigation Toolkit Dependencies
# Install: pip install -r requirements.txt

Flask==3.1.3

# requests is already installed (2.32.5) but listed for completeness
requests>=2.32.0

# pandas is already installed (3.0.1) but listed for future tools
pandas>=3.0.0
```

Key requirements:
- Flask==3.1.3 exact version (verified available in pip)
- requests>=2.32.0 (already installed, listed for documentation)
- pandas>=3.0.0 (already installed, for future tools)
- Comments explaining installation command

Do NOT include development dependencies (pytest, etc.). Do NOT include Flask extensions not needed (Flask-SQLAlchemy, etc.).
  </action>
  <verify>
    <automated>grep -c "Flask==3.1.3" requirements.txt && grep -c "requests" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt exists
    - Contains Flask==3.1.3: grep 'Flask==3.1.3' requirements.txt
    - Contains requests: grep 'requests' requirements.txt
    - Contains pandas: grep 'pandas' requirements.txt
    - File has minimum 5 lines
  </acceptance_criteria>
  <done>requirements.txt created with Flask 3.1.3 and existing dependencies documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create run.bat for Windows startup</name>
  <files>J:\虚拟币犯罪调查工具集\docs\superpowers\run.bat</files>
  <read_first>
    - J:\虚拟币犯罪调查工具集\docs\superpowers\app.py (to verify Flask app structure)
  </read_first>
  <action>Create run.bat Windows startup script:

```batch
@echo off
echo ================================
echo 虚拟币犯罪调查工具集
echo ================================
echo.

REM Check if Flask is installed
pip show Flask >nul 2>&1
if errorlevel 1 (
    echo 正在安装依赖...
    pip install -r requirements.txt
    echo.
)

echo 正在启动Flask服务器...
echo 请在浏览器中访问: http://127.0.0.1:5000
echo 按 Ctrl+C 可停止服务器
echo.

REM Start Flask server
cd /d "%~dp0"
python app.py

pause
```

Key requirements:
- Check Flask installation, install if missing
- Change directory to script location (cd /d "%~dp0")
- Run python app.py
- Display startup message with URL
- pause at end for error visibility

Do NOT use absolute paths. Use %~dp0 for script directory. Do NOT open browser automatically (user may want to see startup messages first). Use python not python3 (Windows convention).

Wait - ROADMAP says "run.bat双击可启动Flask应用" - user should be able to double-click and have it work. Let me also add browser opening:

Updated run.bat with browser opening:
```batch
@echo off
echo ================================
echo 虚币犯罪调查工具集
echo ================================
echo.

REM Check if Flask is installed
pip show Flask >nul 2>&1
if errorlevel 1 (
    echo 正在安装依赖...
    pip install -r requirements.txt
    echo.
)

echo 正在启动Flask服务器...
echo 服务器地址: http://127.0.0.1:5000
echo 按 Ctrl+C 可停止服务器
echo.

REM Change to script directory
cd /d "%~dp0"

REM Start browser after 2 seconds (background)
start "" cmd /c "timeout /t 2 >nul && start http://127.0.0.1:5000"

REM Start Flask server
python app.py

pause
```

Use this version with browser opening.
  </action>
  <verify>
    <automated>grep -c "python app.py" run.bat && grep -c "127.0.0.1:5000" run.bat && grep -c "pip install -r requirements.txt" run.bat</automated>
  </verify>
  <acceptance_criteria>
    - run.bat exists
    - Contains python app.py: grep 'python app.py' run.bat
    - Contains Flask server URL: grep '127.0.0.1:5000' run.bat
    - Contains pip install for dependency check: grep 'pip install -r requirements.txt' run.bat
    - Contains cd /d for directory change: grep 'cd /d' run.bat
    - Browser opening command: grep 'start http://127.0.0.1:5000' run.bat
    - Chinese display text for user guidance
    - Uses @echo off for clean output
  </acceptance_criteria>
  <done>run.bat created for Windows one-click startup with dependency check and browser opening.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create run.sh for Linux/Mac startup</name>
  <files>J:\虚拟币犯罪调查工具集\docs\superpowers\run.sh</files>
  <read_first>
    - J:\虚拟币犯罪调查工具集\docs\superpowers\app.py (to verify Flask app structure)
  </read_first>
  <action>Create run.sh Linux/Mac startup script:

```bash
#!/bin/bash

echo "================================"
echo "虚拟币犯罪调查工具集"
echo "================================"
echo ""

# Check if Flask is installed
if ! pip show Flask > /dev/null 2>&1; then
    echo "正在安装依赖..."
    pip install -r requirements.txt
    echo ""
fi

echo "正在启动Flask服务器..."
echo "服务器地址: http://127.0.0.1:5000"
echo "按 Ctrl+C 可停止服务器"
echo ""

# Change to script directory
cd "$(dirname "$0")"

# Start Flask server
python3 app.py
```

Key requirements:
- Shebang: #!/bin/bash
- Check Flask installation, install if missing
- Change directory to script location (cd "$(dirname "$0")")
- Run python3 app.py (Linux/Mac uses python3)
- Display startup message with URL
- Use pip show to check installation

Do NOT use absolute paths. Use $(dirname "$0") for script directory. Use python3 not python (Linux/Mac convention). Make executable with chmod +x (user responsibility).

Note: Browser opening is optional on Linux/Mac (different systems have different commands - open on Mac, xdg-open on Linux). Keep simple for now.
  </action>
  <verify>
    <automated>grep -c "#!/bin/bash" run.sh && grep -c "python3 app.py" run.sh && grep -c "127.0.0.1:5000" run.sh && grep -c "pip install -r requirements.txt" run.sh</automated>
  </verify>
  <acceptance_criteria>
    - run.sh exists
    - Contains shebang: grep '#!/bin/bash' run.sh
    - Contains python3 app.py: grep 'python3 app.py' run.sh
    - Contains Flask server URL: grep '127.0.0.1:5000' run.sh
    - Contains pip install for dependency check: grep 'pip install -r requirements.txt' run.sh
    - Contains cd for directory change: grep 'cd' run.sh
    - Uses pip show for Flask check
    - Chinese display text for user guidance
    - File has minimum 15 lines
  </acceptance_criteria>
  <done>run.sh created for Linux/Mac startup with dependency check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run.bat -> Python | Script executes Python process |
| run.bat -> Browser | Opens local URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering | Script path | accept | Uses script directory, not absolute path |
| T-01-19 | Elevation of Privilege | pip install | accept | User runs script, same permissions |
| T-01-20 | Spoofing | URL opened | accept | Local URL, no external spoofing |
</threat_model>

<verification>
- run.bat executes without errors on Windows
- Flask installs if missing
- Server starts at 127.0.0.1:5000
- Browser opens to homepage
</verification>

<success_criteria>
- requirements.txt with Flask==3.1.3
- run.bat works on Windows (double-click)
- run.sh works on Linux/Mac (./run.sh)
- Flask installed automatically if missing
- Server starts at correct URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-framework/01-06-SUMMARY.md`
</output>