---
phase: 01-core-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
autonomous: true
requirements: [CORE-03, CORE-05]
user_setup: []

must_haves:
  truths:
    - "User sees sidebar navigation on every page"
    - "User sees legal disclaimer footer on every page"
    - "Page layout has sidebar on left, content on right"
  artifacts:
    - path: "templates/base.html"
      provides: "Base template with sidebar and footer"
      min_lines: 60
      contains: "{% block sidebar %}"
      contains: "{% block content %}"
      contains: "{% block footer %}"
  key_links:
    - from: "templates/base.html"
      to: "child templates"
      via: "Jinja2 {% extends %}"
      pattern: "{% extends 'base.html' %}"
---

<objective>
Create the base HTML template with Tailwind CSS styling, sidebar navigation, and legal disclaimer footer. All pages will inherit from this template.

Purpose: Establish consistent UI foundation for all 11 tools
Output: templates/base.html with sidebar + footer blocks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-framework/01-RESEARCH.md

<interfaces>
<!-- Key patterns from RESEARCH.md Jinja2 template inheritance -->

Tailwind CSS CDN script (exact):
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

Sidebar structure pattern:
<aside class="w-64 bg-gray-800 text-white">
  <nav>
    <a href="/">首页</a>
    <a href="/tron/suspicious-analyzer">TRON可疑分析</a>
  </nav>
</aside>

Legal disclaimer text (exact):
法律声明：本工具仅供合规调查使用，请确保遵守相关法律法规...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create base.html template with Tailwind CSS, sidebar, and footer</name>
  <files>J:\虚拟币犯罪调查工具集\docs\superpowers\templates\base.html</files>
  <read_first>
    - None (new file creation)
  </read_first>
  <action>Create templates/base.html with the following exact structure:

1. Head section:
   - DOCTYPE html, lang="zh-CN"
   - Title block: {% block title %}CIT{% endblock %}
   - Tailwind CDN: <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

2. Body structure (flex layout):
   - <body class="flex min-h-screen bg-gray-100">
   - Sidebar: <aside class="w-64 bg-gray-900 text-white fixed h-full">
   - Main content: <main class="flex-1 ml-64 p-6"> with {% block content %}{% endblock %}
   - Footer: <footer class="fixed bottom-0 left-64 right-0 bg-yellow-100 border-t p-3 text-center text-sm">

3. Sidebar navigation links (exact URLs):
   - "/" -> 首页
   - "/tron/suspicious-analyzer" -> TRON可疑分析
   - "/tron/behavior-analyzer" -> TRON行为分析
   - "/eth/transaction-query" -> ETH交易查询
   - "/trace/uniswap" -> Uniswap追踪
   - "/trace/mixer" -> 混币器追踪
   - "/trace/btc" -> BTC交易分析
   - "/cross/cluster" -> 地址聚类
   - "/cross/cross-border" -> 跨境协查
   - "/case/monitor" -> 多链监控
   - "/case/obfuscation" -> 混淆对抗
   - "/case/asset-freeze" -> 资产追回
   - "/docs/manuals" -> 使用手册

4. Sidebar styling:
   - Logo/title at top: "虚拟币犯罪调查工具集" with h1 class="text-xl font-bold p-4 border-b border-gray-700"
   - Nav links: <nav class="p-4 space-y-2">
   - Each link: <a href="..." class="block py-2 px-4 rounded hover:bg-gray-700 transition">
   - Active state indicator: add class "bg-gray-700" when request.path matches (use Jinja2 request.path)

5. Footer legal disclaimer (exact text):
   法律声明：本工具仅供合规调查使用，请确保遵守相关法律法规。所有分析结果仅供参考，不构成法律证据。使用者需自行承担使用风险。

6. Template blocks:
   - {% block title %}CIT{% endblock %} in head
   - {% block sidebar %}...{% endblock %} in aside (optional override)
   - {% block content %}{% endblock %} in main
   - {% block footer %}...{% endblock %} in footer (optional override)

Do NOT add JavaScript libraries beyond Tailwind. Do NOT use external fonts. Use Tailwind utility classes only.
  </action>
  <verify>
    <automated>grep -c "Tailwind CSS CDN" templates/base.html && grep -c "{% block content %}" templates/base.html && grep -c "法律声明" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - File templates/base.html exists
    - Contains Tailwind CDN script: grep 'cdn.jsdelivr.net/npm/@tailwindcss/browser@4' templates/base.html
    - Contains sidebar block: grep '{% block sidebar %}' templates/base.html OR contains <aside with sidebar nav
    - Contains content block: grep '{% block content %}' templates/base.html
    - Contains footer block: grep '{% block footer %}' templates/base.html OR contains <footer with legal disclaimer
    - Contains exact legal disclaimer: grep '法律声明：本工具仅供合规调查使用' templates/base.html
    - Contains at least 12 navigation links in sidebar
    - File has minimum 60 lines
  </acceptance_criteria>
  <done>Base template created with Tailwind CSS, sidebar navigation (12 tool links), and legal disclaimer footer. All Jinja2 blocks defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template -> Browser | Static HTML served to user browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Sidebar links | accept | Navigation links are internal, no external spoofing risk |
| T-01-02 | Tampering | Legal disclaimer | accept | Static text, no user input |
| T-01-03 | Information Disclosure | Template source | accept | No secrets in template |
</threat_model>

<verification>
- Base template renders without errors
- Sidebar displays 12 tool navigation links
- Footer shows legal disclaimer
- Tailwind CSS applies styling correctly
</verification>

<success_criteria>
- templates/base.html exists with valid HTML structure
- Jinja2 blocks: title, sidebar, content, footer defined
- Sidebar has navigation for all 11 tools + manuals
- Legal disclaimer text present in footer
- Tailwind CDN script included
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-framework/01-01-SUMMARY.md`
</output>