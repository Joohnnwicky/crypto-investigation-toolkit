---
phase: 06-documentation-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/docs/__init__.py
  - modules/docs/routes.py
  - modules/core/exporter.py
  - app.py
autonomous: true
requirements: [EXPORT-03]
user_setup: []

must_haves:
  truths:
    - "User can export analysis results as PDF from any tool result page"
    - "PDF export generates downloadable file with tool name, address short, and date"
    - "PDF endpoint returns valid PDF bytes with Content-Disposition header"
  artifacts:
    - path: "modules/core/exporter.py"
      provides: "PDF export function"
      contains: "def export_pdf"
      min_lines: 10
    - path: "modules/docs/routes.py"
      provides: "PDF export API endpoint"
      contains: "@docs_bp.route('/api/export/pdf'"
      exports: ["docs_bp"]
    - path: "app.py"
      provides: "Blueprint registration"
      contains: "app.register_blueprint(docs_bp)"
  key_links:
    - from: "modules/docs/routes.py"
      to: "modules/core/exporter.py"
      via: "import export_pdf"
      pattern: "from modules.core.exporter import export_pdf"
    - from: "app.py"
      to: "modules/docs/routes.py"
      via: "blueprint registration"
      pattern: "register_blueprint(docs_bp)"
---

<objective>
Build PDF export infrastructure: WeasyPrint integration, PDF export endpoint, docs Blueprint registration.

Purpose: Enable all 11 tools to offer PDF export alongside JSON/CSV (per D-01, D-02).
Output: export_pdf function in exporter.py, /docs/api/export/pdf endpoint, docs Blueprint registered in app.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-export/06-CONTEXT.md
@.planning/phases/06-documentation-export/06-RESEARCH.md
@.planning/phases/06-documentation-export/06-PATTERNS.md
</context>

<interfaces>
<!-- Key types and patterns executor needs from existing codebase -->

From modules/core/exporter.py (lines 79-93):
```python
def get_export_filename(address: str, format_type: str, analysis_type: str = 'analysis') -> str:
    from datetime import datetime
    date_str = datetime.now().strftime('%Y%m%d')
    addr_short = address[:8] + address[-4:] if len(address) > 12 else address
    return f"tron_{analysis_type}_{addr_short}_{date_str}.{format_type}"
```

From modules/tron/routes.py (lines 67-72):
```python
response = Response(
    content,
    mimetype='application/json',
    headers={'Content-Disposition': f'attachment; filename="{filename}"'}
)
return response
```

From app.py (lines 10-17):
```python
from modules.tron.routes import tron_bp
from modules.eth.routes import eth_bp
# ... other blueprints ...
app.register_blueprint(tron_bp)
app.register_blueprint(eth_bp)
```

From modules/tron/routes.py (line 8):
```python
tron_bp = Blueprint('tron', __name__, url_prefix='/tron')
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create docs module structure with Blueprint</name>
  <files>modules/docs/__init__.py, modules/docs/routes.py</files>
  <read_first>
    - modules/tron/routes.py (Blueprint pattern)
    - modules/core/exporter.py (existing exports to extend)
  </read_first>
  <action>Create modules/docs/__init__.py with module docstring "Documentation and PDF export modules". Create modules/docs/routes.py with Flask Blueprint named docs_bp with url_prefix='/docs'. Include placeholder for PDF export endpoint (will be filled in Task 2). Do NOT create any page routes yet - this task only sets up the Blueprint structure. Pattern follows modules/tron/routes.py line 8: Blueprint('docs', __name__, url_prefix='/docs').
  </action>
  <verify>
    <automated>grep -n "docs_bp = Blueprint" modules/docs/routes.py && grep -n "url_prefix='/docs'" modules/docs/routes.py</automated>
  </verify>
  <acceptance_criteria>
    - modules/docs/__init__.py exists with content "Documentation and PDF export modules"
    - modules/docs/routes.py contains docs_bp = Blueprint('docs', __name__, url_prefix='/docs')
    - docs_bp variable defined at module level (not inside function)
  </acceptance_criteria>
  <done>docs Blueprint created with correct url_prefix '/docs', ready for endpoint registration.</done>
</task>

<task type="auto">
  <name>Task 2: Add PDF export function to exporter.py</name>
  <files>modules/core/exporter.py</files>
  <read_first>
    - modules/core/exporter.py (existing export functions to extend)
  </read_first>
  <action>Extend modules/core/exporter.py by adding export_pdf function after get_export_filename (after line 93). Function signature: export_pdf(data: Dict[str, Any], tool_type: str) -> bytes. Implementation: import weasyprint.HTML, create simple HTML template string with basic result display, convert to PDF bytes using HTML(string=html_content).write_pdf(). Also add get_pdf_filename(address: str, tool_type: str) -> str following existing filename pattern. Do NOT use Flask render_template - use simple string template to avoid template dependency. The HTML template should include: title with tool name, basic_info grid, alerts sections (red/yellow/green). Use inline CSS for styling (not Tailwind CDN since PDF generation happens server-side).
  </action>
  <verify>
    <automated>grep -n "def export_pdf" modules/core/exporter.py && grep -n "def get_pdf_filename" modules/core/exporter.py && grep -n "from weasyprint import HTML" modules/core/exporter.py</automated>
  </verify>
  <acceptance_criteria>
    - modules/core/exporter.py contains def export_pdf(data: Dict[str, Any], tool_type: str) -> bytes
    - modules/core/exporter.py contains def get_pdf_filename(address: str, tool_type: str) -> str
    - export_pdf function imports from weasyprint import HTML
    - export_pdf function returns bytes type
  </acceptance_criteria>
  <done>export_pdf and get_pdf_filename functions added to exporter.py, WeasyPrint integration complete.</done>
</task>

<task type="auto">
  <name>Task 3: Create PDF export endpoint in routes.py</name>
  <files>modules/docs/routes.py</files>
  <read_first>
    - modules/tron/routes.py (export endpoint pattern lines 48-72)
    - modules/core/exporter.py (newly added export_pdf function)
  </read_first>
  <action>Add PDF export endpoint to modules/docs/routes.py. Route: @docs_bp.route('/api/export/pdf', methods=['POST']). Handler: validate request has 'result' and 'tool_type', call export_pdf(result, tool_type), generate filename with get_pdf_filename, return Response with mimetype='application/pdf' and Content-Disposition header. Pattern matches modules/tron/routes.py lines 48-72 (export_json_endpoint). Import export_pdf and get_pdf_filename from modules.core.exporter. Handle error case: if missing result or tool_type, return jsonify({'error': '请提供分析结果和工具类型'}) with 400 status.
  </action>
  <verify>
    <automated>grep -n "@docs_bp.route('/api/export/pdf'" modules/docs/routes.py && grep -n "mimetype='application/pdf'" modules/docs/routes.py</automated>
  </verify>
  <acceptance_criteria>
    - modules/docs/routes.py contains @docs_bp.route('/api/export/pdf', methods=['POST'])
    - Endpoint imports export_pdf and get_pdf_filename from modules.core.exporter
    - Response has mimetype='application/pdf'
    - Response has Content-Disposition header with filename
    - Error handling returns 400 status for missing data
  </acceptance_criteria>
  <done>PDF export endpoint functional at /docs/api/export/pdf, ready for frontend integration.</done>
</task>

<task type="auto">
  <name>Task 4: Register docs Blueprint in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py (existing Blueprint registrations)
  </read_first>
  <action>Extend app.py: add import statement for docs_bp after line 8: from modules.docs.routes import docs_bp. Add Blueprint registration after line 17: app.register_blueprint(docs_bp). Follow exact pattern of existing imports and registrations. Do NOT modify any existing lines - only add new import and registration.
  </action>
  <verify>
    <automated>grep -n "from modules.docs.routes import docs_bp" app.py && grep -n "app.register_blueprint(docs_bp)" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py contains from modules.docs.routes import docs_bp
    - app.py contains app.register_blueprint(docs_bp) after other blueprint registrations
    - Import placed in import block (lines 4-8 area)
    - Registration placed in registration block (lines 12-17 area)
  </acceptance_criteria>
  <done>docs Blueprint registered, PDF export endpoint accessible at /docs/api/export/pdf.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client to /docs/api/export/pdf | Untrusted JSON input (analysis result data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | /docs/api/export/pdf endpoint | mitigate | Validate 'result' and 'tool_type' present, reject malformed JSON with 400 |
| T-06-02 | Denial | PDF generation | accept | WeasyPrint handles large HTML; no explicit limit needed for local tool |
| T-06-03 | Information Disclosure | PDF filename | mitigate | Sanitize address in filename - use alphanumeric characters only (addr_short pattern already safe) |
</threat_model>

<verification>
1. Python imports valid: `python -c "from modules.docs.routes import docs_bp; from modules.core.exporter import export_pdf"`
2. Blueprint accessible: Flask app starts without error
3. Endpoint responds: POST to /docs/api/export/pdf with valid JSON returns PDF bytes
</verification>

<success_criteria>
- docs Blueprint created and registered in app.py
- export_pdf function added to exporter.py using WeasyPrint
- /docs/api/export/pdf endpoint functional and returns PDF download
- All 4 tasks pass their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-export/06-01-SUMMARY.md`
</output>