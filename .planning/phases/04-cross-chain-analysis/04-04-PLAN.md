---
phase: 04-cross-chain-analysis
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - modules/cross/routes.py (partial - cross-border endpoints added)
  - templates/cross/cross_border.html
autonomous: true
requirements: [CROSS-02]
user_setup: []

must_haves:
  truths:
    - "User sees step-by-step form (D-19: Case Info -> Address Selection -> Template Generation)"
    - "User can import addresses from cluster result (D-20)"
    - "User can fill case info fields (D-15)"
    - "User can fill background fields (D-17)"
    - "User can select request type (D-18)"
    - "User sees generated HTML template"
    - "User can copy template as plain text (D-13)"
  artifacts:
    - path: "templates/cross/cross_border.html"
      provides: "Cross-border coordination frontend UI"
      contains: "step indicators, case info form, address selection, background fields, request fields, template display, copy button"
    - path: "modules/cross/routes.py"
      provides: "Cross-border API endpoints"
      exports: ["cross_border_generate", "cross_border_page"]
  key_links:
    - from: "templates/cross/cross_border.html"
      to: "sessionStorage"
      via: "Import from cluster"
      pattern: "sessionStorage.getItem.*cit_cluster_export"
    - from: "templates/cross/cross_border.html"
      to: "/cross/api/cross-border/generate"
      via: "fetch POST request"
      pattern: "fetch.*cross-border/generate"
</objective>

<objective>
Build cross-border coordination frontend page with step-by-step form and template generation.

Purpose: Provide user interface for international investigation coordination template creation.
Output: cross_border.html template and cross-border API endpoints in routes.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Template structure from D-14 to D-18 -->

Step-by-step form structure per D-19:
```
Step 1: 案件基本信息 (D-15)
  - case_number: 案件编号
  - agency: 调查机构
  - contact_person: 联系人
  - contact_method: 联系方式

Step 2: 涉案地址选择 (D-16)
  - addresses: 涉案地址列表 (import from cluster OR manual input)
  - chain_types: 链类型 (auto-detected)
  - total_amount: 金额汇总
  - tx_hashes: 交易哈希

Step 3: 调查背景与请求 (D-17, D-18)
  - suspicious_behavior: 可疑行为描述
  - fund_flow: 资金流向简述
  - investigation_context: 调查背景
  - request_type: 请求类型 (下拉选择: "信息查询", "资产冻结", etc.)
  - expected_response: 期望回复时间
```

From modules/cross/cross_border_generator.py (created in Plan 02):
```python
def generate_template_web(fields: Dict[str, Any]) -> Dict[str, Any]
# Returns: {"success": bool, "template_data": dict, "plain_text": str}
```

From templates/eth/transaction_query.html (session cache restore pattern):
```javascript
const CACHE_KEY = 'cit_eth_query';
window.onload = function() {
    const cached = sessionStorage.getItem(CACHE_KEY);
    if (cached) {
        const data = JSON.parse(cached);
        displayResults(data);
    }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-border API endpoints to routes</name>
  <files>modules/cross/routes.py</files>
  <read_first>
    - modules/cross/routes.py (existing cluster endpoints from Plan 03)
    - modules/cross/cross_border_generator.py (generate_template_web function)
  </read_first>
  <behavior>
    - Test 1: POST /cross/api/cross-border/generate with all fields returns {"success": true, "template_data": {...}}
    - Test 2: POST /cross/api/cross-border/generate with missing fields returns {"success": false, "error": "缺少必填字段: ..."}
    - Test 3: GET /cross/cross-border renders cross_border.html template
  </behavior>
  <action>
    Add cross-border endpoints to modules/cross/routes.py (append to existing file).

    @cross_bp.route('/api/cross-border/generate', methods=['POST'])
    def cross_border_generate():
    - Parse JSON body with all template fields (D-14 to D-18):
      - case_number, agency, contact_person, contact_method
      - addresses, chain_types, total_amount, tx_hashes
      - suspicious_behavior, fund_flow, investigation_context
      - request_type, expected_response
    - Import generate_template_web from .cross_border_generator
    - Call generate_template_web(data)
    - Return jsonify(result) or jsonify(result), 400 based on success

    @cross_bp.route('/cross-border')
    def cross_border_page():
    - Return render_template('cross/cross_border.html')

    Note: Blueprint already created in Plan 03, just add new endpoints.
  </action>
  <verify>
    <automated>grep -n "def cross_border_generate" modules/cross/routes.py && grep -n "def cross_border_page" modules/cross/routes.py</automated>
  </verify>
  <done>
    - cross_border_generate endpoint at /cross/api/cross-border/generate
    - cross_border_page endpoint at /cross/cross-border
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cross-border frontend template</name>
  <files>templates/cross/cross_border.html</files>
  <read_first>
    - templates/base.html (lines 1-68: base template structure)
    - templates/eth/transaction_query.html (lines 117-130: sessionStorage restore pattern)
    - modules/cross/chain_detector.py (detect_chain_type for auto-detecting imported addresses)
  </read_first>
  <behavior>
    - Test 1: Page shows 3-step form with step indicators
    - Test 2: Import from cluster button retrieves sessionStorage.cit_cluster_export
    - Test 3: Step 1 form has case_number, agency, contact_person, contact_method fields
    - Test 4: Generate template button triggers POST to /cross/api/cross-border/generate
    - Test 5: Template display shows structured HTML
    - Test 6: Copy button copies plain_text to clipboard
  </behavior>
  <action>
    Create templates/cross/cross_border.html extending base.html.

    Page structure per D-19 step-by-step form:

    1. Header: "跨境协查模板生成" title, description about international coordination

    2. Step indicators:
       - Visual progress indicator showing Step 1/2/3
       - Current step highlighted
       - Back/Next buttons for navigation

    3. Step 1: 案件基本信息 (D-15):
       - case_number input: placeholder="例如: 案件编号-2024-001"
       - agency input: placeholder="例如: XX市公安局"
       - contact_person input: placeholder="例如: 张三"
       - contact_method input: placeholder="例如: xxx@police.gov.cn"

    4. Step 2: 涉案地址选择 (D-16):
       - Import from cluster button per D-20:
         - onclick="importFromCluster()"
         - Reads sessionStorage.getItem('cit_cluster_export')
         - Extracts addresses and chain_types from cluster result
         - Populates address list
       - Manual address input textarea (alternative to import):
         - id="manual-addresses-input"
         - placeholder="每行一个地址（支持TRON/ETH/BTC）"
       - Address list display:
         - Shows imported/manually entered addresses
         - Auto-detects chain types using JavaScript (TRON T-prefix, ETH 0x-prefix, BTC 1/3/bc1)
       - total_amount input: placeholder="例如: 50000 USDT"
       - tx_hashes textarea: placeholder="每行一个交易哈希"

    5. Step 3: 调查背景与请求 (D-17, D-18):
       - suspicious_behavior textarea: placeholder="描述可疑行为..."
       - fund_flow textarea: placeholder="简述资金流向..."
       - investigation_context textarea: placeholder="调查背景..."
       - request_type select dropdown:
         - Options: "信息查询", "资产冻结", "交易记录调取", "其他"
       - expected_response input: placeholder="例如: 7个工作日内"

    6. Generate template section:
       - Generate button: onclick="generateTemplate()"
       - Triggers POST to /cross/api/cross-border/generate with all fields

    7. Template display per D-13:
       - id="template-display"
       - Structured HTML layout showing all template sections:
         - 案件基本信息 section
         - 涉案地址信息 section (table format)
         - 调查背景说明 section
         - 协查请求内容 section
         - Footer: Generated time + disclaimer
       - Copy button per D-13:
         - onclick="copyTemplate()"
         - Uses navigator.clipboard.writeText(plain_text)
         - Shows success message after copy

    8. Loading indicator and error display (standard pattern)

    JavaScript functions:
    - navigateToStep(step): Show/hide step sections, update indicators
    - importFromCluster(): Read sessionStorage.cit_cluster_export, extract addresses
    - detectAddressChain(address): JavaScript version of chain detection
    - generateTemplate(): Collect all fields, POST to API
    - displayTemplate(data): Render template HTML from template_data
    - copyTemplate(): Copy plain_text to clipboard

    Use Tailwind CSS classes matching existing templates.

    Note: No JSON/CSV export per D-13 - only HTML display + text copy.
  </action>
  <verify>
    <automated>grep -n "Step 1" templates/cross/cross_border.html && grep -n "importFromCluster" templates/cross/cross_border.html && grep -n "generateTemplate" templates/cross/cross_border.html && grep -n "copyTemplate" templates/cross/cross_border.html && grep -n "sessionStorage.getItem.*cit_cluster_export" templates/cross/cross_border.html</automated>
  </verify>
  <done>
    - templates/cross/cross_border.html exists extending base.html
    - 3-step form with step indicators (per D-19)
    - Import from cluster button reads sessionStorage (per D-20)
    - All template fields implemented (D-15 to D-18)
    - Template display section (per D-13)
    - Copy button for plain text (per D-13)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Template field data crosses here |
| sessionStorage → Frontend | Cluster import data from previous tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Template fields | mitigate | Backend validates required fields before processing |
| T-04-09 | Information Disclosure | Generated template | accept | Template is user's investigation data, no system secrets |
</threat_model>

<verification>
- Cross-border endpoints accessible
- Template renders correctly
- Step navigation works
- Import from cluster reads sessionStorage
- Generate template triggers API
- Template display shows structured content
- Copy button works
</verification>

<success_criteria>
- modules/cross/routes.py with cross-border endpoints
- templates/cross/cross_border.html with step-by-step form
- All D-13 to D-21 requirements implemented in frontend
- Import from cluster functional (D-20)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-chain-analysis/04-04-SUMMARY.md`
</output>