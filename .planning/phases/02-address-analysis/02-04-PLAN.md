---
phase: 02-address-analysis
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - app.py
autonomous: true
requirements:
  - ADDR-03
  - ADDR-04
  - ADDR-05
user_setup: []
must_haves:
  truths:
    - "TRON behavior analyzer accessible from sidebar link"
    - "ETH transaction query accessible from sidebar link"
    - "All tools work end-to-end when Flask app runs"
    - "No import errors or route conflicts"
  artifacts:
    - path: "app.py"
      provides: "Flask app with all blueprints registered"
      contains: "eth_bp"
      exports: ["app.register_blueprint(eth_bp)"]
  key_links:
    - from: "app.py"
      to: "modules/eth/routes.py"
      via: "from modules.eth.routes import eth_bp"
      pattern: "from modules.eth.routes import"
    - from: "app.py"
      to: "modules/tron/routes.py"
      via: "existing import"
      pattern: "from modules.tron.routes import"
---

<objective>
Integrate TRON behavior analyzer and ETH module into Flask app, verify end-to-end functionality.

Purpose: Register new blueprints, add page routes, and confirm all Phase 2 tools work.
Output: Updated app.py with complete integration, verified working tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-address-analysis/02-CONTEXT.md

<!-- Prior plan SUMMARYs needed for integration -->
@.planning/phases/02-address-analysis/02-01-SUMMARY.md
@.planning/phases/02-address-analysis/02-02-SUMMARY.md
@.planning/phases/02-address-analysis/02-03-SUMMARY.md

<interfaces>
<!-- Key exports from prior plans -->

From modules/tron/routes.py (extended in 02-01):
```python
tron_bp = Blueprint('tron', __name__, url_prefix='/tron')
# New routes: /behavior-analyzer, /api/behavior, /api/export/behavior/json, /api/export/behavior/csv
```

From modules/eth/routes.py (created in 02-02):
```python
eth_bp = Blueprint('eth', __name__, url_prefix='/eth')
# Routes: /api/query, /transaction-query, /api/export/json, /api/export/csv
```

From app.py (current state):
```python
from flask import Flask, render_template
from modules.tron.routes import tron_bp

app = Flask(__name__)
app.register_blueprint(tron_bp)

@app.route('/')
@app.route('/tron/suspicious-analyzer')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.py to register eth_bp and add page routes</name>
  <files>app.py</files>
  <read_first>
    - app.py (current state, add new imports and routes)
    - modules/eth/routes.py (eth_bp to import)
    - modules/tron/routes.py (existing tron_bp, new routes added in 02-01)
  </read_first>
  <action>
Update app.py to integrate Phase 2 modules:

1. Add import for eth_bp:
```python
from modules.eth.routes import eth_bp
```

2. Register eth_bp after tron_bp:
```python
app.register_blueprint(eth_bp)
```

3. Add page route for ETH transaction query:
```python
@app.route('/eth/transaction-query')
def eth_transaction_query():
    return render_template('eth/transaction_query.html')
```

Note: TRON behavior-analyzer route is already in tron_bp (added in 02-01), so no new @app.route needed for that.

File structure after update:
- Imports: Flask, render_template, tron_bp, eth_bp
- Blueprint registration: tron_bp, eth_bp
- Page routes: /, /tron/suspicious-analyzer
- (TRON behavior-analyzer handled by tron_bp.route)
- (ETH transaction-query handled by @app.route and eth_bp.route)
</action>
  <verify>
    <automated>grep -c "eth_bp" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py contains `from modules.eth.routes import eth_bp`
    - app.py contains `app.register_blueprint(eth_bp)`
    - app.py contains `@app.route('/eth/transaction-query')` OR route exists in eth_bp
    - No syntax errors when running: `python -c "from app import app; print('OK')"`
    - Total blueprint registrations >= 2 (tron_bp + eth_bp)
  </acceptance_criteria>
  <done>
app.py updated with eth_bp registration and ETH page route. All Phase 2 modules integrated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 2 address analysis tools integrated into Flask app:
1. TRON behavior analyzer (4 behavior patterns, Summary Cards UI)
2. ETH transaction query (Etherscan API, API key per-request, Stargate detection)
3. Both tools accessible from sidebar navigation
  </what-built>
  <how-to-verify>
1. Start Flask app: `python app.py` (or run.bat)
2. Visit http://127.0.0.1:5000 - verify sidebar shows "TRON行为分析" and "ETH交易查询" links
3. Click "TRON行为分析" - verify page loads with 4 Summary Cards layout
4. Enter sample TRON address (TUtPdo7L45ey2KrpibdNcjNL3ujqXo1NNw), click "开始分析"
5. Verify results show: 首次资金来源, 交易模式, 地址关系, 活动时间线 cards
6. Click "ETH交易查询" - verify page loads with address + API key inputs
7. Enter ETH address (0x...) and valid Etherscan API key, click "开始查询"
8. Verify transaction list shows, and any Stargate events appear in separate card
9. Test export buttons (JSON/CSV) for both tools
  </how-to-verify>
  <resume-signal>Type "approved" if all tools work, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Integration does not add new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Denial of Service | Flask app startup | accept | Standard Flask error handling, no new risks from integration |
</threat_model>

<verification>
- Flask app starts without errors: `python app.py`
- All imports resolve: `from app import app`
- Sidebar links work: TRON behavior analyzer, ETH transaction query
- Both tools functional end-to-end
</verification>

<success_criteria>
- eth_bp registered in app.py
- TRON behavior analyzer accessible from sidebar
- ETH transaction query accessible from sidebar
- Both tools work end-to-end (address input -> API call -> results display -> export)
- All Phase 2 requirements (ADDR-03, ADDR-04, ADDR-05) validated
</success_criteria>

<output>
After completion, create `.planning/phases/02-address-analysis/02-04-SUMMARY.md`
</output>