---
phase: 02-address-analysis
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - templates/eth/transaction_query.html
autonomous: true
requirements:
  - ADDR-04
  - ADDR-05
user_setup: []
must_haves:
  truths:
    - "User sees address input field and API key input field on ETH query page"
    - "API key placeholder shows helpful text about obtaining key from etherscan.io"
    - "User sees transaction list with card-based layout"
    - "User sees Stargate bridge events highlighted in separate card section"
  artifacts:
    - path: "templates/eth/transaction_query.html"
      provides: "ETH transaction query UI"
      contains: "API密钥"
      min_lines: 250
  key_links:
    - from: "templates/eth/transaction_query.html"
      to: "/eth/api/query"
      via: "fetch POST with address and api_key"
      pattern: "fetch.*'/eth/api/query'"
    - from: "templates/eth/transaction_query.html"
      to: "/eth/api/export/json"
      via: "exportJSON function"
      pattern: "exportJSON"
---

<objective>
Create ETH transaction query frontend with API key input per D-07, D-08, ADDR-05.

Purpose: Provide user interface for ETH transaction queries with cross-chain bridge detection display.
Output: templates/eth/transaction_query.html with card-based UI and API key input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-address-analysis/02-CONTEXT.md
@.planning/phases/02-address-analysis/02-RESEARCH.md

<!-- Prior plan SUMMARY reference needed because we use eth_analyzer exports -->
@.planning/phases/02-address-analysis/02-02-SUMMARY.md

<interfaces>
<!-- Key types from prior plan 02-02 -->

From modules/eth/eth_analyzer.py (created in 02-02):
```python
def query_eth_transactions_web(address: str, api_key: str) -> Dict[str, Any]:
    # Returns: {success, address, transactions: {normal, erc20, total_count}, stargate_events, error}

def is_valid_eth_address(address: str) -> bool:
    # Pattern: r'^0x[a-fA-F0-9]{40}$'
```

From modules/eth/routes.py (created in 02-02):
```python
eth_bp = Blueprint('eth', __name__, url_prefix='/eth')

@eth_bp.route('/api/query', methods=['POST'])  # Request: {address, api_key}
@eth_bp.route('/transaction-query')  # Page route
@eth_bp.route('/api/export/json', methods=['POST'])
@eth_bp.route('/api/export/csv', methods=['POST'])
```

From templates/tron/suspicious_analyzer.html (UI pattern per D-07):
- Header card pattern
- Input section with label + input + buttons
- Loading spinner
- Error display
- Results cards with grid layout
- Export buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create transaction_query.html with API key input and results layout</name>
  <files>templates/eth/transaction_query.html</files>
  <read_first>
    - templates/tron/suspicious_analyzer.html (UI pattern to follow per D-07, D-08)
    - templates/base.html (base template to extend)
    - modules/eth/routes.py (endpoints to call)
    - .planning/phases/02-address-analysis/02-RESEARCH.md (API key input pattern per D-04)
  </read_first>
  <action>
Create templates/eth/transaction_query.html implementing ETH UI per D-07, D-08:

Structure (extends base.html):
1. Header card: "ETH交易查询" + description "查询以太坊交易历史，识别Stargate跨链桥事件"

2. Input card per D-07, ADDR-05:
   - ETH address input field (placeholder: "例如：0x1234...")
   - API key input field (placeholder: "从 etherscan.io 获取免费API密钥" per Pitfall 1 in RESEARCH.md)
   - "开始查询" button (no sample button - API key required per query)

3. Loading indicator (reuse pattern from suspicious_analyzer.html)

4. Error display (reuse pattern)

5. Results section per D-08 (card-based):
   - Basic info card: address, total transaction count
   - Transactions card: table/list showing recent transactions (hash, from, to, value, timestamp)
   - Stargate events card (if any): highlighted with bridge icon, shows tx_hash, contract_type, timestamp
   - Use bg-blue-50 for Stargate section (distinct from red/yellow/green alerts)

6. Export buttons: JSON + CSV

JavaScript:
- queryTransactions(): fetch '/eth/api/query' POST with {address, api_key}
- displayResults(data): populate cards
- show Stargate events in separate section if stargate_events.length > 0
- exportJSON/exportCSV: call '/eth/api/export/json' and '/eth/api/export/csv'

Important: API key is sent per-request only, never stored in localStorage or variable between queries.
</action>
  <verify>
    <automated>grep -c "api_key" templates/eth/transaction_query.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/eth/transaction_query.html exists
    - Contains `{% extends "base.html" %}`
    - Contains `{% block title %}ETH交易查询{% endblock %}`
    - Contains input field with id for ETH address
    - Contains input field with id for API key (placeholder mentions etherscan.io)
    - Contains function `queryTransactions()` in JavaScript
    - Contains fetch to `/eth/api/query` with both address and api_key in body
    - Contains section for Stargate events (id like 'stargate-events')
    - Contains export buttons
    - Total lines >= 200 (matching suspicious_analyzer.html complexity)
    - No localStorage usage for API key
  </acceptance_criteria>
  <done>
ETH transaction query frontend created per D-07, D-08. API key input per D-04, ADDR-05 with helpful placeholder.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Flask | User input (ETH address, API key) crosses trust boundary |
| Browser local | API key should NOT persist in localStorage/sessionStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | API key in browser | mitigate | No localStorage/sessionStorage for API key, only per-request POST body (per ADDR-05) |
| T-02-03-02 | Tampering | address input | accept | Backend validates with regex, XSS handled by Jinja2 auto-escape |
</threat_model>

<verification>
- UI renders: visit http://127.0.0.1:5000/eth/transaction-query shows page
- API key input field visible with placeholder text
- Address input field visible
</verification>

<success_criteria>
- ETH transaction query UI created matching D-07 layout
- API key input field with helpful placeholder per D-04, ADDR-05
- Card-based results layout per D-08
- Stargate events section distinct from transaction list
- No API key storage in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-address-analysis/02-03-SUMMARY.md`
</output>